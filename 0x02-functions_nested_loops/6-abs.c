#include "main.h"


/**
 * _abs - compute the absolute value of int
 * @n: character to be compute
 * Return: if @n is negtive it's compute to a positive
 */
int _abs(int n)
{
	if  (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
